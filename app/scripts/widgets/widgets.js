
define([

    'underscore',

    'app'

], function(_, App) {

    'use strict';

    if (require === undefined) return;

    function campusmap_(el, settings) {

        require([], function () {

        });

    }
 
    return {

        Campusmap: campusmap_

    };

});
